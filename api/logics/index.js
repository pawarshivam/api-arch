const ping   = global.absolute('api/logics/ping');
const user   = global.absolute('api/logics/user');
const org    = global.absolute('api/logics/org');

/* ++++++++++++++++++++++++++++++++++++++++++++++++ */
module.exports = {
  ping,
  user,
  org,
};
