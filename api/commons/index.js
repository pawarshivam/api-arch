const config = global.absolute('api/commons/config');
const env    = global.absolute('api/commons/env');

/* ++++++++++++++++++++++++++++++++++++++++++++++++ */
module.exports = {
  config,
  env,
};
