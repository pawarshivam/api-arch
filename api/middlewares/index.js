const integration = global.absolute('api/middlewares/integration');
const error       = global.absolute('api/middlewares/error');

/* ++++++++++++++++++++++++++++++++++++++++++++++++ */
module.exports = {
  integration,
  error,
};
